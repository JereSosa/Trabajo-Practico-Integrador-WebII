extends ../layout

block contenido
  .flex.items-center.justify-center.h-screen.bg-gradient-to-br.from-red-50.to-blue-100
    .bg-white.rounded-xl.shadow-lg.p-8.max-w-md.w-full
      h2.text-2xl.font-bold.mb-6.text-red-600 Emergencia – Asignar cama
      form(method="POST" action="/emergencia/asignar")
        label.block.mb-2.font-semibold.text-gray-800(for="motivo") Motivo de la emergencia:
        textarea#motivo(name="motivo" required rows="3" class="w-full border p-2 rounded mb-4 text-gray-900")

        label.block.mb-2.font-semibold.text-gray-800(for="sexo") Sexo del paciente:
        select(name="sexo" required class="w-full border p-2 rounded mb-4 text-gray-900")
          option(value="" disabled selected) Seleccionar…
          option(value="M") Masculino
          option(value="F") Femenino

        button(
          type="submit"
          class="bg-red-500 hover:bg-red-600 text-white font-bold py-2 px-4 rounded w-full"
        ) Registrar y asignar cama de emergencia

